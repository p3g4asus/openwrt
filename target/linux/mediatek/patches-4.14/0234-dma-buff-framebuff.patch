--- a/drivers/base/Kconfig	2019-11-19 23:28:07.000000000 +0100
+++ b/drivers/base/Kconfig	2019-11-20 11:43:36.000000000 +0100
@@ -244,13 +244,13 @@
 	select GLOB
 
 source "drivers/base/regmap/Kconfig"
 
 config DMA_SHARED_BUFFER
 	tristate
-	default n
+	default m
 	select ANON_INODES
 	select IRQ_WORK
 	help
 	  This option enables the framework for buffer-sharing between
 	  multiple drivers. A buffer is associated with a file using driver
 	  APIs extension; the file's descriptor can then be passed on to other
--- a/mm/Kconfig	2019-10-29 09:17:49.000000000 +0100
+++ b/mm/Kconfig	2019-11-20 11:42:53.000000000 +0100
@@ -741,12 +741,13 @@
 	  Allows creation of struct pages to represent addressable device
 	  memory; i.e., memory that is accessible from both the device and
 	  the CPU
 
 config FRAME_VECTOR
 	bool
+	default y
 
 config ARCH_USES_HIGH_VMA_FLAGS
 	bool
 config ARCH_HAS_PKEYS
 	bool
 
